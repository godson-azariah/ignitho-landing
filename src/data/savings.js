/* The two assumptions behind any savings figure quoted on the site.

   They used to live in the savings calculator, which has been taken off the
   page. The fourth step picture still quotes them, and a number that appears on
   screen belongs with the rest of the words rather than inside a section that
   might be removed again. */
export const SAVINGS_PER_EMPLOYEE = 320;
export const DEFAULT_EMPLOYEES = 500;
